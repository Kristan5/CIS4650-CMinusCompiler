/* this file is trying to index a non-array variable */

void main(void) {
  int x;
  x = input();
  fac = 1;
  
  while (x > 1) {
    fac = fac * x;
    x = x - 1;
    fac[1];
  } 

  output(fac);
}
